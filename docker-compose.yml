version: '3.9'


services:
  backend-account:
    container_name: account
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app

    ports:
      - "8000:8000"

    environment:
      - SECRET_KEY=test
      - DEBUG=True